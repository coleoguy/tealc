/* Intentionally empty.
   Previously injected a brand block into <body>, which Chainlit 2.11 rendered
   below the whole chat as a full-size image. Logo is now served via CSS
   pseudo-elements in stylesheet_v2.css (body::before) — no JS needed. */
